import React from "react"
import { navigate } from "gatsby"
import styles from "../styles.module.scss"
import Layout from "../components/layout"

const QuestList = () => {

  return (
    <Layout>
      <p>Currently there are no quests in your area.</p>
    </Layout>
  )
}

export default QuestList
